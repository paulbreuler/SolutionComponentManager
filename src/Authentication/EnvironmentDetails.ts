/**
 * Power Apps environment information
 * TODO may not be needed as IAuthParams requires resource, which may 
 * be expected as CRM Url?
 */
export interface IEnvironmentDetails {
    org_url: string
}